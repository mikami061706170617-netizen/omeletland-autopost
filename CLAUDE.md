# ⚠ ここは「インスタ自動投稿」のフォルダです

Omelet Land Tbilisi の Instagram / Facebook 自動投稿一式。
**AI CAD（AICAD）とは全く別の仕事です。** このフォルダの外は触らないこと。

# このリポジトリを触るときの約束

- **依存パッケージを増やさない。** Python標準ライブラリのみ。pipは使わない。
- **リポジトリは Public のまま。** Private にすると IG API が画像を取得できず投稿が止まる。
- **トークンをコミットしない。** 値は GitHub Secrets にだけ置く。
- 変更したら必ず `python3 test_logic.py` を通す。
- `state.json` は Actions が書き換える。手で編集するのは、人が手動投稿した日を
  `history` に足すときだけ。
- 画像は 1080×1350（4:5）JPEG。差し替えるときも同じ寸法にする。
- キャプションは 2200文字以内・ハッシュタグ30個以内。
- 写真は実写のものだけを使う（AI生成に見えるものはSNSに出さない）。

## リールを扱うときの追加ルール
- 動画は **必ず 1080×1920（縦）**。横素材から作る場合は `scale=-2:1920,crop=1080:1920` で中央を切り抜く
  （ぼかしの帯は入れない。流用に見える）。
- 撮影素材（`動画/`）は絶対にコミットしない。数GBある。
- 同じリールを二度出さない。`state.json` の `reels_history` で管理する。
- AI生成の映像は使わない（実写のみ）。日本の店の映像もトビリシのアカウントには使わない。
